# Review remediation — 2026-09-06

The earlier review and Cambrian trial remain historical evidence. This document
maps their findings to the resulting changes; detailed execution and closure
are recorded in [LN-0013](../../lab-journal/journal-2026-09-06-close-real-content-gaps.md).

| Finding | Resolution |
|---|---|
| Missing portable license | Root MIT license travels with the starter, skill, and bundled starter; byte parity is checked. |
| Overbroad instrumentation closure | COR-0251-01 narrows the conclusion to failure without hooks. The broader timing question remains open, with an owner role and discriminating test. Original Markdown is retained. |
| Examples lag the full protocol | A complete current full-format reference supplements the historical specimens; documentation distinguishes their roles. Routine work has a compact reference. |
| HTML closure omits verification limits | Template now asks for actual checks, omitted checks, scope limits, unresolved questions, next entry, and signature method. |
| Shallow integrity validation | Gate checks unique live IDs, archive counts/rows, complete new-record identity, actual local fragments and duplicate anchors. Preserved-record hashes reject rewrites while permitting append-only additions. Negative fixtures exercise failures. |
| Narrow accessibility and browser evidence | Automated A/AA scans include both archives, template, live plate, two specimens and three Cambrian samples at phone/desktop sizes. Reader tasks and real-content regressions run in Chromium, Firefox and WebKit; exhaustive geometry and screenshots remain Chromium checks. |
| Broken mobile metadata labels; witness ambiguity | Wider label column allows word-boundary wrapping; indexes distinguish design acceptance from independent witness evidence. |
| Weak actual-host evidence | Real host attempts replace discovery-only assumptions. Codex adoption and correction are exercised in an isolated fixture. Claude access is blocked; Grok headless execution cancels. See the retained host report for exact scope. |
| Mobile code/log/diff overflow | Shared bounded code regions preserve source whitespace, accept keyboard focus, and wrap for print. Real-source hashes and code contents are regression checked. |
| Excess routine-page ceremony | Routine sample uses a compact reading view without an unnecessary diagram; Markdown remains the recommended routine record. |
| Uniform print columns | Wide comparison matrix gives dimension labels 28% of width and data columns 9% each; print no longer breaks those labels into fragments. |
| Unclear retrospective conversion | Protocol and skill require pinned source provenance, labelled additions, missing-metadata disclosures, and retention of original claims/signatures as attribution. |
| Mixed evidence under reading lenses | Imported mixed sections appear in both evidence and synthesis lenses with explicit labels. Historical estimates are marked as such. |
| Source inconsistency | The source's “three changes” / four-item mismatch remains preserved and disclosed, rather than silently edited. |

The expanded accessibility run found and fixed two additional keyboard-inaccessible
tables in the template and live plate. Visual baselines were reviewed and deliberately
updated for the wider identity labels and dated correction notice. Final gates run
without updating snapshots.

Hosted CI and user design acceptance remain separate from local verification.
There is no claim that a skill guarantees every model's output or that automated
accessibility checks replace a complete human assistive-technology audit.
