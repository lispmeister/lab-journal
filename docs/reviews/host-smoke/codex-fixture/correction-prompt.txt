Use $lab-journal. This is the second part of the isolated skill smoke test. Read fixture-legacy.md: it deliberately contains a false signed specimen claim, clearly labelled synthetic. Inspect check.py to determine its actual scope. Create a full append-only correction record that precisely identifies that claim and distinguishes it from the accurate limitations in your existing LN-0001. Preserve fixture-legacy.md and all existing signed entries byte-for-byte. Update both notebook indexes and relevant ledgers. Do not invent browser results, witnesses, timestamps, or historical predictions. Run only the existing local check if useful. No dependencies, external services, commits or delegation. Report actual results and limitations.